using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ludo_waypoints : MonoBehaviour
{
    GameObject LudoSpawn;
    // Start is called before the first frame update
    void Start()
    {
        LudoSpawn = new GameObject();
        LudoSpawn.name = "ludoObj1";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
